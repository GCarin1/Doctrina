[1mResuming change 0001-add-a-billing-invoice-export[22m[90m — agent-executed (ADR 0005).[39m

[1mWork playbook — change 0001-add-a-billing-invoice-export[22m[90m — agent-executed (ADR 0005).[39m

Prompt: "add a billing invoice export"
Likely capabilities (deterministic term match — a hint, not a decision):
    [36mbilling             [39m score 110   [90m.doctrina/specs/billing/spec.md[39m

Execute in order, in a single linear pass:

1. Read the context pack and confirm (or correct) the capability:
       [36mdoctrina context billing --concat[39m

2. If the capability has no spec yet:
       [36mdoctrina spec new <capability>[39m
[90m   Trace it to product intent: tag the product.md success-criteria bullet[39m
[90m   it serves with an anchor ("- [SC1] ...") and set the spec's[39m
[90m   "**Realizes:** SC1" header (or "n/a — <why>"). `doctrina validate` warns[39m
[90m   on an active spec with no Realizes; `doctrina trace` reports the link.[39m

3. A delta is already scaffolded (Operation prefilled) at
   .doctrina/changes/0001-add-a-billing-invoice-export/specs/billing/delta.md — fill its body.
[33m   It was RANKED, not pinned — confirm the capability first; the file[39m
[33m   says how to correct it if the ranking got it wrong.[39m
   Add one more delta per additional affected capability:
[90m       # Spec Delta — capability: <capability>[39m
[90m       **Operation:** ADDED | MODIFIED | REMOVED[39m
[90m       **Target spec on apply:** `.doctrina/specs/<capability>/spec.md`[39m
[90m       ---[39m
[90m       <EARS body. Keep the two axes honest (Status vs Implementation),[39m
[90m        keep aspiration under ## Maturity → Future, and cite the proof per[39m
[90m        criterion: "1. [unverified] <signal> — verified by `path/to/test`">[39m
[90m   New capability? `spec new` then an ADDED delta with the FULL body — apply[39m
[90m   replaces the untouched scaffold. Bookkeeping edits? MODIFIED with an ops[39m
[90m   block, applied mechanically (all ops or none):[39m
[90m       ```ops[39m
[90m       set-header Implementation: verified — `src/x.js`[39m
[90m       bump-version minor[39m
[90m       set-criterion 1: verified[39m
[90m       append-criterion [unverified] new signal — verified by `test/y.test.js`[39m
[90m       append-requirement event: When <trigger>, the system shall <action>.[39m
[90m       replace-requirement ubiquitous 2: The system shall <action>.[39m
[90m       ```[39m
[90m   append-* ops number/position at APPLY time, so concurrent changes[39m
[90m   appending to the same spec never collide. Only free-prose rewrites[39m
[90m   (Purpose, Maturity, ...) stay a by-hand merge.[39m

4. Replace the placeholder tasks in .doctrina/changes/0001-add-a-billing-invoice-export/tasks.md
   with small, checkable implementation tasks (a few hours each, max),
   and record the change's What/Scope in its proposal.md. Do this BEFORE
   implementing — analyze and close refuse a change whose scaffold
   placeholders were never replaced, and `change tick` will not tick them.

5. Implement task by task, checking each box as it lands. Advance the
   spec's Implementation: planned → partial → implemented as code lands.
   If the prompt is genuinely ambiguous, ask the user before assuming.

6. ADR checkpoint — does this change decide something structural
   (an architecture, a boundary, a trade-off a future session must
   not relitigate)? If yes: [36mdoctrina decision new "<title>"[39m now,
   before closing — the definition of done requires it recorded.
[90m   (close re-checks this: it warns when the change touches capabilities[39m
[90m   cited by an accepted ADR — amend via decision supersede, not silence.)[39m
   Touching an integration surface (ports, env vars, public endpoints)?
   Own it in a contract: [36mdoctrina contract new <id>[39m · [36mdoctrina contract check[39m.

7. Close in one attested pass (preferred — runs every gate and stops
   at the first failure with the exact rerun command):
       [36mdoctrina close 0001-add-a-billing-invoice-export[39m
[90m   It self-reviews on the way through: the close runs `doctrina review`[39m
[90m   before the apply, where its findings can still change what gets[39m
[90m   written. Advisory — it reports and the close continues.[39m
[90m   (equivalent, step by step: [39m[90manalyze → change apply → verify → coverage → trace → change archive → validate)[39m
   Before it: check the proposal's ## Verification boxes and every task,
   closing steps included, and bump Implementation to verified.

8. [36mdoctrina next[39m for the follow-up.
[90m   If this change taught a reusable lesson (a fix you'd hate to relearn,[39m
[90m   a recurring convention), capture it: [39m[36mdoctrina skill new <slug>[39m[90m.[39m
