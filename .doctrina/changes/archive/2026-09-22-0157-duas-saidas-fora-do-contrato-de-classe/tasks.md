# Tasks — Change 0157-duas-saidas-fora-do-contrato-de-classe

- [x] Percorrer a fronteira inteira num teste — todo comando, fora de um
      projeto — em vez de perguntar comando a comando.
- [x] Deixar um erro tipado atravessar o driver de múltiplos ids com a
      classe intacta, mantendo o relato por id dos erros não tipados.
- [x] Trocar pelas chamadas ao helper compartilhado as últimas cópias à mão
      da precondição, e perguntar pelo projeto antes da referência.
- [x] Devolver a classe de precondição, com hint, quando não há repositório
      git onde instalar o hook.
- [x] Fazer a regra da spec nomear as exceções que o CLI realmente tem.

## Closing steps

- [x] Apply the change: merge each delta into the corresponding spec.
- [x] Archive the change folder to `.doctrina/changes/archive/2026-09-22-0157-duas-saidas-fora-do-contrato-de-classe/`.
- [x] Update `.doctrina/index.json` with new or modified artifacts.
