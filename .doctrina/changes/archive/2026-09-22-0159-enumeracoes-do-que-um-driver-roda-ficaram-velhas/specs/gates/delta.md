# Spec Delta — capability: gates

**Operation:** MODIFIED
**Target spec on apply:** `.doctrina/specs/gates/spec.md`

---

A regra que a change 0150 escreveu para quem MOSTRA uma sequência vale
igual para quem a ENUMERA em prosa: o autor é a declaração.

```ops
append-requirement ubiquitous: The system shall hold every prose enumeration of what a driver runs to that driver's own declaration — the gate sequence for a sequence, the shipped template for a hook — so that an abridged list is a failure rather than a reading.
append-criterion [verified] The contract's description of the composite action names every step of the declared CI sequence — verified by `packages/doctrina-cli/test/o-que-um-driver-roda-tem-um-autor.test.js`.
bump-version minor
```
