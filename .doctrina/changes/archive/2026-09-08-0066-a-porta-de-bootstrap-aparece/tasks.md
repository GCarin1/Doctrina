# Tasks — Change 0066-a-porta-de-bootstrap-aparece

<!--
Each task is a single checkable item. Keep tasks small (under a few hours
of work). The change is done when every box is checked, including the
three closing steps at the bottom.
-->

<!-- Planejado, nao iniciado. Retome com `doctrina work --resume 0066-a-porta-de-bootstrap-aparece`. -->

- [x] Decidir a saída: o `init` deixa o `intake.md` em `pending`, ou o caminho de leitura passa a nomear o `intake` quando não há spec nenhuma. Registrar a decisão na proposta.
- [x] Executar a decisão e conferir que o gatilho do `AGENTS.md` corresponde ao que fica em disco.
- [x] Conferir que um projeto já convertido, e um que adotou por `--from-diff`, não são reapontados.
- [x] Teste: `init` real, e o estado imediatamente seguinte.
- [x] Escrever o corpo EARS do delta `specs/scaffolding`.

## Closing steps

- [x] Apply the change: merge each delta into the corresponding spec.
- [x] Archive the change folder to `.doctrina/changes/archive/2026-09-08-0066-a-porta-de-bootstrap-aparece/`.
- [x] Update `.doctrina/index.json` with new or modified artifacts.
