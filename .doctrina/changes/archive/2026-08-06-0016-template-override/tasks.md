# Tasks — Change 0016-template-override

<!--
Each task is a single checkable item. Keep tasks small (under a few hours
of work). The change is done when every box is checked, including the
three closing steps at the bottom.
-->

- [x] `lib/templates.js`: per-file resolution chain + resolved listing.
- [x] Route spec/skill/decision/contract scaffolders through it; report the source.
- [x] `templates list`: label each entry, mark overrides.
- [x] Tests: override wins, per-file fallback, empty dir unchanged.
- [x] ADR 0019 accepted.
- [x] Docs: templates.md EN+PT + sidebars.

## Closing steps

- [x] Apply the change: merge each delta into the corresponding spec.
- [x] Archive the change folder to `.doctrina/changes/archive/2026-08-06-0016-template-override/`.
- [x] Update `.doctrina/index.json` with new or modified artifacts.
