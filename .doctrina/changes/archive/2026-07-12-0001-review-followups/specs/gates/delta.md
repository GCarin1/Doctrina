# Spec Delta — capability: gates

**Operation:** MODIFIED
**Target spec on apply:** `.doctrina/specs/gates/spec.md`

Prose additions (merged by hand — new EARS items): clarify language lexicons
+ `clarify:ok` suppression; coverage `deferred` verdict, `--only` scoping,
and `--run` via the declared `evidence_runner`; `close` coverage scoped to
the change's touched capabilities; `validate` enforcement of
`.doctrina/rules.json` (ADR 0014).

```ops
bump-version minor
```

---

<!-- Prose requirements merged into the target spec by hand; see the
     Event-driven items tagged (ADR 0014). -->
