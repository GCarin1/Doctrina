# Tasks — Change 0055-comentario-nao-e-conteudo

<!-- Planejado, não iniciado. Retome com `doctrina work --resume 0055-comentario-nao-e-conteudo`. -->

- [x] Escrever o helper que remove intervalos de comentário HTML antes de qualquer leitura, no document model.
- [x] Aplicar em `documentedSurfaceSignals` (docs-impact) e confirmar que o delta por palpite deixa de gerar sinal.
- [x] Aplicar em `parseRequirements` (show) e confirmar que R1 é o primeiro requisito real numa spec recém-scaffoldada.
- [x] Explicar no `--help` do show como a numeração dele se relaciona com a do `replace-requirement`.
- [x] Testes: spec criada por `spec new` (não as deste repo), change com delta por palpite, e o caso já coberto do bloco de ops.
- [x] Escrever os corpos EARS dos deltas `specs/validation`, `specs/insight` e `specs/gates`.

## Closing steps

- [x] Apply the change: merge each delta into the corresponding spec.
- [x] Archive the change folder to `.doctrina/changes/archive/2026-09-08-0055-comentario-nao-e-conteudo/`.
- [x] Update `.doctrina/index.json` with new or modified artifacts.
