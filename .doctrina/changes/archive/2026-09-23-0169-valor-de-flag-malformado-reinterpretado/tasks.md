# Tasks — Change 0169-valor-de-flag-malformado-reinterpretado

<!--
Each task is a single checkable item. Keep tasks small (under a few hours
of work). The change is done when every box is checked, including the
three closing steps at the bottom.
-->

- [x] `parsePositiveInt` e valor negativo depois de flag em `args.js`.
- [x] `sinceWindow` em `metrics-model.js`; `metrics` recusa antes de olhar o histórico.
- [x] `report`, `status` e `context` usam `parsePositiveInt`.
- [x] Teste `um-valor-malformado-e-recusado.test.js`, provado vermelho nos comandos anteriores.
- [x] Help e docs EN/PT do `metrics --since`; delta em `cli`; CHANGELOG.

## Closing steps

- [x] Apply the change: merge each delta into the corresponding spec.
- [x] Archive the change folder to `.doctrina/changes/archive/2026-09-23-0169-valor-de-flag-malformado-reinterpretado/`.
- [x] Update `.doctrina/index.json` with new or modified artifacts.
