# Tasks — Change 0166-intent-add-aceita-a-mesma-intencao-duas-vezes

<!--
Each task is a single checkable item. Keep tasks small (under a few hours
of work). The change is done when every box is checked, including the
three closing steps at the bottom.
-->

- [x] `intentKey` e o aviso de gêmea no `validate` (`validation-model.js`).
- [x] `intent add` recusa um texto que já tem âncora (`intent.js`).
- [x] Teste `uma-intencao-nao-vira-duas-ancoras.test.js`, provado vermelho no código anterior.
- [x] Deltas em `authoring` e `gates`; entrada no CHANGELOG.

## Closing steps

- [x] Apply the change: merge each delta into the corresponding spec.
- [x] Archive the change folder to `.doctrina/changes/archive/2026-09-23-0166-intent-add-aceita-a-mesma-intencao-duas-vezes/`.
- [x] Update `.doctrina/index.json` with new or modified artifacts.
