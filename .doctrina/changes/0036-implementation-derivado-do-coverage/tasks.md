# Tasks — Change 0036-implementation-derivado-do-coverage

<!-- Planejado, não iniciado. Retome com `doctrina work --resume 0036-implementation-derivado-do-coverage`. -->

- [ ] Escrever a regra de derivação e expô-la no `summarize` de `coverage.js`.
- [ ] Adicionar o aviso de contradição ao `validate`, respeitando a nota de escape.
- [ ] Fazer o `close` propor o op `set-header Implementation:` na etapa consultiva.
- [ ] Implementar `spec set <cap> --implementation auto`.
- [ ] Testes: as três faixas da regra, a nota de escape, e o op proposto.
- [ ] Atualizar `docs/en/gating.md` e `docs/pt/gating.md`.
- [ ] Escrever o corpo EARS do delta `specs/gates/delta.md`.

## Closing steps

- [ ] Apply the change: merge each delta into the corresponding spec.
- [ ] Archive the change folder to `.doctrina/changes/archive/2026-09-07-0036-implementation-derivado-do-coverage/`.
- [ ] Update `.doctrina/index.json` with new or modified artifacts.
