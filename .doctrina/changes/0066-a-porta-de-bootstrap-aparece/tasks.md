# Tasks — Change 0066-a-porta-de-bootstrap-aparece

<!--
Each task is a single checkable item. Keep tasks small (under a few hours
of work). The change is done when every box is checked, including the
three closing steps at the bottom.
-->

<!-- Planejado, nao iniciado. Retome com `doctrina work --resume 0066-a-porta-de-bootstrap-aparece`. -->

- [ ] Decidir a saída: o `init` deixa o `intake.md` em `pending`, ou o caminho de leitura passa a nomear o `intake` quando não há spec nenhuma. Registrar a decisão na proposta.
- [ ] Executar a decisão e conferir que o gatilho do `AGENTS.md` corresponde ao que fica em disco.
- [ ] Conferir que um projeto já convertido, e um que adotou por `--from-diff`, não são reapontados.
- [ ] Teste: `init` real, e o estado imediatamente seguinte.
- [ ] Escrever o corpo EARS do delta `specs/scaffolding`.

## Closing steps

- [ ] Apply the change: merge each delta into the corresponding spec.
- [ ] Archive the change folder to `.doctrina/changes/archive/2026-09-08-0066-a-porta-de-bootstrap-aparece/`.
- [ ] Update `.doctrina/index.json` with new or modified artifacts.
