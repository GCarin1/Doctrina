# Spec Delta — capability: api-quota

**Operation:** ADDED
**Target spec on apply:** `.doctrina/specs/api-quota/spec.md`

---

(Body merged into the materialised spec; canonical content lives
at `.doctrina/specs/api-quota/spec.md`. The materialised spec
covers more than the immediate bug because the team chose to
spec the whole capability while it had the file open — a common
brownfield pattern when the bug is in code the team understands.)
