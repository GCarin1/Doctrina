# Tasks — Change 0001

- [x] Draft `proposal.md` and spec delta.
- [x] Add `GET /{code}` route in `app/main.py`.
- [x] Add `Storage.get(code)` in `app/storage.py`.
- [x] Confirm POST → GET round-trip works end-to-end.

## Closing steps

- [x] Apply the change.
- [x] Archive the change folder.
- [x] Update `.doctrina/index.json` with the new spec.
